{
  "name": "chasse-aux-renards",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy",
    "deploy:test": "vite build && firebase hosting:channel:deploy test-channel",
    "emulator": "./scripts/start-emulator.sh",
    "seed": "node scripts/seed.js"
  },
  "devDependencies": {
    "@vite-pwa/assets-generator": "^1.0.0",
    "firebase-admin": "^13.4.0",
    "firebase-tools": "^14.24.0",
    "sharp": "^0.34.3",
    "vite": "^7.0.4",
    "vite-plugin-pwa": "^1.0.2"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "firebase": "^12.0.0",
    "material-symbols": "^0.40.2"
  }
}
